document.addEventListener("DOMContentLoaded",()=>{if(PPStore.needsInitialSetup()){location.href="configuracao-inicial.html";return;}if(PPStore.hasAdminSession()){location.href="dashboard.html";return;}const form=document.querySelector("#admin-login");form.addEventListener("submit",e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form).entries());const r=PPStore.loginStaff(d.username,d.password);const error=document.querySelector("[data-login-error]");error.textContent="";if(r.ok){location.href="dashboard.html";return;}error.textContent=r.reason==="inactive"?"Este acesso está desativado. Entre em contato com a administração.":"Credenciais inválidas.";});});