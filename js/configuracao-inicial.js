document.addEventListener("DOMContentLoaded",()=>{if(!PPStore.needsInitialSetup()){location.href="login.html";return;}const form=document.querySelector("#initial-setup-form");form.addEventListener("submit",e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form).entries());const error=document.querySelector("[data-setup-error]");error.textContent="";if(d.password.length<8){error.textContent="A senha deve possuir pelo menos 8 caracteres.";return;}if(d.password!==d.confirmPassword){error.textContent="As senhas não coincidem.";return;}const r=PPStore.createInitialAdmin(d);if(!r.ok){error.textContent="A configuração inicial já foi concluída.";return;}PPStore.loginStaff(d.username,d.password);location.href="dashboard.html";});});